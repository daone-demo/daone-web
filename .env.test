# 测试环境

# 当前环境标识
VITE_APP_ENV = 'test'

# 测试打包/联调默认走同源相对前缀；本地 vite --mode test 时由代理转发到 VITE_API_BASE_HOST
# （IP 为自签证书时，浏览器直连 https 会 ERR_CERT_AUTHORITY_INVALID）
VITE_API_BASE_URL = '/api/api/v1'

# 仅作开发代理上游 origin（不要带 /api 路径）
VITE_API_BASE_HOST = 'https://43.161.199.75:8088'
